version: '3.9'
services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=kameleoon'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5433:5432'
